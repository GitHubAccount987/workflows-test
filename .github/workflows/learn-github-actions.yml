name: my-first-workflow
run-name: first-run
on: [issues]

jobs:
  first-job:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-verssion: '20'
      - run: node index.js
